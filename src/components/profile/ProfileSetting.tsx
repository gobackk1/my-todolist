import React from 'react'
import { Button } from '@material-ui/core'

export const ProfileSetting: React.FC = () => {
  return (
    <>
      <Button disabled={true}>メールアドレスを変更する(実装予定)</Button>
      <br />
      <Button disabled={true}>パスワードを変更する(実装予定)</Button>
    </>
  )
}
